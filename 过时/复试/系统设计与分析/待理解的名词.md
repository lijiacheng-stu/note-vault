- 封装
将对象的属性和行为结合成为一个独立的单位，对外屏蔽其内部细节。
- 继承
特殊类拥有其一般类的全部属性与操作，称作特殊类对一般类的继承。
- *多态* (重写，动态绑定？，类属？)
在一般类中定义的属性或操作被特殊类继承之后，可以具有不同的数据类型或表现出不同的行为。可以通过重写、动态绑定、类属的机制来实现。

聚合
关联
消息通讯

- 基类
从一个类派生出另一个类时，原始类称为基类
- 派生类
继承类称为派生类

- 适配器
我现在的系统用的是A接口，有个服务它的接口时B形态，怎样才能不改变A接口的情况下，适用B接口的服务呢？