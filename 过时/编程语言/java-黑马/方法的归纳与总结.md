### List集合的5种遍历方式
#### 方式
- 迭代器
- 增强for
- 普通for
- lambda表达式
- 列表迭代器
#### 优劣比较
![[Pasted image 20240327230304.png]]
#### 代码演示 
```java
  
public class MathTest {  
    public static void main(String[] args) {  
//       1、学习lambda表达式  
//       2、自己演示遍历5种遍历方法  
        List<String> ls=new ArrayList<>();  
        ls.add("aaa");  
        ls.add("bbb");  
        ls.add("ccc");  
        ls.add("ddd");  
        System.out.println(ls);  
        //普通的遍历方式-->索引遍历  
        for (int i = 0; i < ls.size(); i++) {  
            System.out.println(ls.get(i));  
        }  
  
        //增强for  
        for (String s : ls) {  
            System.out.println(s);  
        }  
        //用迭代器  
        Iterator<String> it=ls.iterator();  
        while(it.hasNext()){  
            System.out.println(it.next());  
        }  
        //ListIterator  
        ListIterator<String> litr=ls.listIterator();  
        while (litr.hasNext()){  
            System.out.println(litr.next());  
        }  
        //lambda表达式  
        ls.forEach(s-> System.out.println(s));  
    }  
  
}
```





### 对数组或者集合进行规范地打印
```java
将数组打印成
[1,2,3,5,6,4]
```
#### 对于 CharSequence型的数组使用StringJoiner可能比较好。
![[Pasted image 20240331165419.png]]
理由：创建StringJoiner对象时，对加入的每一个对象就定义好了prefix,suffix,delimiter
![[Pasted image 20240331165701.png]]
![[Pasted image 20240331165711.png]]
![[Pasted image 20240331165725.png]]
```java
public class Test2 {  
    public static void main(String[] args) {  
        String[] arr={"hello","hi","xiaoming","nishishei"};  
        StringJoiner sj=new StringJoiner(",","[","]");  
        for (int i = 0; i < arr.length; i++) {  
            sj.add(arr[i]);  
        }  
        System.out.println(sj);  
    }  
}
```
#### 终极方法：Arrays.toString(object[] a)






### 使用第三方包的步骤
![[Pasted image 20240406111301.png]]
### 制造假数据--网络爬虫

### IDEA中让一个代码跑多次，而不是覆盖上一次，只运行一个实例
**应用场景**
- 让多个程序发送程序同时向一个接收程序发送数据
- 让一个发送数据向多个接收程序接收数据
**操作**
点击edit configrations进入下面界面->点击后选择 allow mutiple instance
![[Pasted image 20240407212656.png]]
