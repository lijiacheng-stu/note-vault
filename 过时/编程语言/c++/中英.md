- macro 宏
- recursive 递归的
- viable 切实可行的
- alias 别名
- VPtr = vtable pointer 虚表指针
- ellipse 椭圆
- fixed address 固定的地方
- LIFO=last in first out 后进先出(形容析构函数的调用次序)
- portable 可移植的
- refer to 引用
- STL = Standard Template Library 标准模板库 (通过using namespace std使用）
- preserve 保持（preserve number of operands）
- operand 操作数
- operator 操作符
- override 重写
- overload 重载
- prefix 前缀
- postfix 后缀