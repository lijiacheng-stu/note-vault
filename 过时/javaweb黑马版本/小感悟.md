### 通用
1、对于异常，除了可以抛出外，还可以直接抓取。这是对异常的两种处理方式
2、对于释放资源，一定要执行，所以往往放在finally语句中，但存在两个问题：
- 由于局部变量的原因，无法在finally语句中访问。所以得把资源变量try catch finaly句式前面，赋初始值为Null。 
- 由于资源申请之前就出现了异常，这个时候如果.close()将产生空指针异常，所以得再close之前判断资源变量是否为null
例如:
```java
  
public class JdbcDemo1 {  
    public static void main(String[] args) {  
        Connection conn=null;  
        Statement statement=null;  
        ResultSet result=null;  
        try {  
            //1.注册驱动  
            Class.forName("com.mysql.cj.jdbc.Driver");  
            //2.获取连接  
            conn = DriverManager.getConnection("jdbc:mysql://localhost:3306/db4", "root", "123456");  
            //3.定义sql  
//           String sql = "alter table account add id int;";  
//            String sql = "alter table account modify id int primary key auto_increment";  
//            String sql = "insert into account values ('xiaofang',3000,null)";  
            String sql="select * from account";  
            //4.获取statemStatement statementent对象  
            statement= conn.createStatement();  
            //5.执行sql  
            result = statement.executeQuery(sql);  
            while(result.next()){  
                int id = result.getInt("id");  
                String name = result.getString(1);  
                double money = result.getDouble(2);  
                System.out.println(id+"--"+name+"--"+money);  
            }  
           //6.释放资源  
            statement.close();  
            conn.close();  
        } catch (Exception e) {  
            throw new RuntimeException(e);  
        }finally {  
            if(result!=null){  
                try {  
                    result.close();  
                } catch (SQLException e) {  
                    throw new RuntimeException(e);  
                }  
            }  
            if(statement!=null){  
                try {  
                    statement.close();  
                } catch (SQLException e) {  
                    throw new RuntimeException(e);  
                }  
            }  
            if(conn!=null){  
                try {  
                    conn.close();  
                } catch (SQLException e) {  
                    throw new RuntimeException(e);  
                }  
            }  
  
  
        }  
    }  
}
```

### HTML+CSS+JS
#### HTML
1、布局的确定：在不考虑css的情况下，通过表格table的嵌套实现布局