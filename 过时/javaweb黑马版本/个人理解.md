### 类的对象和类的Class对象之间是什么关系？
- 类对象可以有多个，但是类的Class对象只有一个
- 类对象描述的是这个类对象的属性，类的Class对象描述的是类这一特殊对象的属性，对于这个类来讲，具有一般性
- getClass方法，类名.class,Class.forName("全类名")的返回值都是对象是Class对象，而对象就是在堆中吧。

### 如何理解编解码和乱码问题？包括mine类型，charset之类的赋值？感觉很乱！
