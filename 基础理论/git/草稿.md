commit object:
- pointer to the snapshot of this commit
- author's name and email address
- commit message
- pointer of parents

tree object：
- table that lists the contents of directory and specifies which file names are stored as whichd blobs 

Centralized Workflow：
- 所有的开发者都能向中心仓库推送
Integration-Manager Workflow：
- 只有integration manager才能推送到中心仓库
- 其他开发者只能推送到自己的库中
Dictator and Lieutenants Workflow：
- 只有dictator才能推送推送到中心库
- 设置了中尉