技巧：
-  You can even include spaces in a filename by typing a backslash (\) before the space.

原则：
 - The editor normally won’t let you throw away your edits.

知识：
1. unix系统的文件名的要求：, a filename can include any 8-bit character except a slash (/), which is reserved as the separator between files and directories in a pathname, and ASCII NUL, the character with all zero bits.
2. 