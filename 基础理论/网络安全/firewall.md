why use a firewall：
- Firewalls can also be used for content filtering.
- Firewalls can intercept incoming malicious traffic before it reaches the network, as well as prevent sensitive information from leaving the network.