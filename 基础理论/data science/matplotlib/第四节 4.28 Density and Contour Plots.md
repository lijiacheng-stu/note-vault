对np.meshgrid的理解：
对于绘制一个登高线图有a set of (x_i, y_j, f(x_i, y_j)) `[其中(x_i, y_j)∈x和y的笛卡尔积]`即可。
x和y的笛卡尔积得到的点，在二维平面上是有`len(y) * len(x)`个点的网格。怎样保存的这些点呢？要知道 怎样才能保存这种前后左右的位置关系呢？设定了X,Y两个矩阵变量，X，Y分别只存储这些点的横坐标和纵坐标。这样点就存储好了。`f(X,Y)` 的操作，可以返回一个矩阵，



理解鸿沟在于：
Z是一个矩阵，由(i, j)进行索引。z_i,j是(x_i,j, y_i,j)的函数。


`plt.cm` cm = color map


contour三个Z.shape的表格。(i,j)对应于从左下角到右上角。每个位置得填上该点的x，y和f(x,y)的值。


高斯平滑后面的数学原理