1. pandas对缺失数据的处理方式：
**传统的对缺失数据的处理方式：** 
- 方法一：using a _mask_ that globally indicates missing values
- 方法二：choosing a _sentinel value_ that indicates a missing entry.
**权衡：**
- 方法一：增加了额外的存储和计算开销
- 方法二：减少了有效数据的表示范围，需要额外的逻辑计算
**结果：（两者都用）**
- The default mode is to use a sentinel-based missing data scheme, with sentinel values `NaN` or `None` depending on the type of the data.
- Alternatively, you can opt in to using the nullable data types (dtypes) Pandas provides (discussed later in this chapter), which results in the creation an accompanying mask array to track missing entries. These missing entries are then presented to the user as the special `pd.NA` value.

2. 必要的背景知识：
None: python object
NaN：floating-point value
NA：For types that don’t have an available sentinel value, Pandas automatically typecasts when NA values are present.

NA的必要性：
In early versions of Pandas, `NaN` and `None` as sentinel values were the only missing data representations available. The primary difficulty this introduced was with regard to the implicit type casting: **for example, there was no way to represent a true integer array with missing data.** 


3. numpy和pandas在数组中存在多种数据类型时（尤其包括np.NaN,None,pd.NA时）数组类型的处理是不同的：
- numpy中的规则：
```
1.出现多种数据类型时，通过以下方式upcast，直到出现第一个交点：
*boolean
*integer
*floating
*object

2.NAs对应的类型：
None：object
NaN：float
1：int
1.3： float
True： boolean
'hello': object

3.numpy中仅仅涉及了None，NaN两种缺失值
```

pandas中的规则：
- 规则一：
```
1.不出现NAs时，对数据类型的处理
|*integer
|*floating
*|boolean
|/
*object
```
- 规则二：在一的基础上，出现NAs的处理方式
![[Pasted image 20251012161933.png]]

- 规则三：
```
原因：上面两个规则下，The primary difficulty this introduced was with regard to the implicit type casting: for example, there was no way to represent a true integer array with missing data.

手段：提出了 nullable dtypes，也就是pd.NA

操作：将所有的NAs都看成时pd.NA。（定义时，对应的类型加上大写。？） + 其他细节
```


2. 处理Null values:(都是object method)
- detect null values：
	- isnull
	- notnull
- drop null values:
	- dropna
- filling null values：
	- fillna



