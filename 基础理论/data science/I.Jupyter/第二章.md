相比于知道这个问题的答案，更重要的是在短时间内找到未知的答案
相比于记住工具，更重要的是知道找到这些信息的方式

**Python object（Python 对象）**  
在 Python 中，几乎所有东西都是对象，包括：
- 函数（function）
- 类（class）
- 模块（module）
- 方法（method）
- 变量（value，例如字符串、整数、列表等）

为什么Ipython和jupyter比python强？

原因一：Python lets you use and explore Python efficiently and interactively.
section 1
`?` 能够找到Python object所指向doctring，而docstring中包含了这个对象的用法。
`??` 能够显示源码。如果不显示 ，说明不是用纯python写得，可能使用了C或者其他语言。
`<TAB>`自动补齐。Though Python has no strictly enforced distinction between public/external attributes and private/internal attributes, by convention a preceding underscore is used to denote the latter.自动补齐的时候，自动忽略私有方法和内部属性。这些方法方法叫double underscore（dunder）方法
`wildcard matching` 上面的tab只有在知道头几个有效的时候才有用，这个方法，知道中间的也行。`*` 代表一个或多个字符

section 2
一些常用的shortcut，快捷方式:（以下方法在shell中可以用，notebook中不一定能用）
- navigation 
- text entry
- command history 
- miscellaneous

原因二： `magic commands`, 增强了python语法
-  `%`单行 `%%` 多行
- `%run` 执行外部文件
- `%timeit` 计算性能
- `%magic``%lsmagic` 可以探索其他的


原因三：In and Out Objects用于记录输入和输出历史


原因四：Ipython能能够实现在ipython中运行系统命令行，且结果可以和原python互动
The magic happens with the exclamation point: anything appearing after ! on a line will be executed not by the Python kernel, but by the system command line.






