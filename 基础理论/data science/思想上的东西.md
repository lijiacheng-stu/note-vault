python的设计哲学`Code is read much more often than it is written` 。
直接上来就能用的`list,dic,str,type` 它们其实不是方法，而是类！而且一般内部类会用小写，其他的则驼峰命名法表示类。这样的作法，说是更好读。

接手一个数据集的操作：
- 查看承接数据集的变量的数据类型，有了数据类型，我们可以根据其特性进行探索。
- 将一个数据集读为一个dataframe后，我们可以通过shape属性查看数据规模，可以通过`pd.iloc[0]`或`pd.head()`来查看具体的样子。
- 探索每一个属性的意义
