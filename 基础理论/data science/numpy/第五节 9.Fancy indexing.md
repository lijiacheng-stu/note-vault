疑问：  ind数组只能是numpy array吗？ 
答案： 都行 list和numpy array都行

When using arrays of indices, the shape of the result reflects the shape of the _index arrays_ rather than the shape of the _array being indexed_

结果的结果和索引的形状有关，和被索引的对象的形状无关。