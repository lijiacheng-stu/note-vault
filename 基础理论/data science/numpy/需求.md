#### 需求一： 生成一些测试用的numpy array
##### 1. 对一维度：
- 连续的整数：`x = np.arange(5)`
- 随机数：
	- `rng = np.random.default_rng(seed=1701)`
	- `x = rng.integers(10, size=5)`
##### 2.对二维度：
- 随机数：
	- M = rng.integers(0, 10, (3, 4))