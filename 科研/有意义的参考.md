0也没有影响，因为要均值化。



# 方案2：Prototype Prompt Pool（我最看好）
一个类别，有N个典型的原型。通过I_L分别与N个原型比较相似度，然后在对者N个相似度进行聚合(可以用注意力池化，既然可以保留亮点信息，又可以兼顾其他其他次要信息，表达能力更强)，得到这个类别最终相似度。其中，每个类别的典型的原型是手工输入的。如，邮轮，军舰，渔船，游艇。


现在最重要的问题是，CLIP是基于自然图像的预训练模型，在遥感场景下，语义空间存在差异。

有没有基于自然图像的语义分割呢？

- 第一篇文章基于CLIP的自然图像的语义分割
- 第二篇文章是基于自然语言的CLIP2RS




这是我觉得最适合遥感分割的。

---

每个类别维护：

NNN

个prompt。

例如：

Ship：

Tship={t1,t2,t3,t4}T_{ship} = \{ t_1,t_2,t_3,t_4 \}Tship​={t1​,t2​,t3​,t4​}

---

然后：

Global Feature：

IGI_GIG​

作为Query。

---

Prompt Pool：

TshipT_{ship}Tship​

作为Key。

---

计算：

α=softmax(IGTshipT)\alpha = softmax(I_GT_{ship}^T)α=softmax(IG​TshipT​)

---

得到：

```
cargo ship  0.7warship     0.1cruise ship 0.1fishing     0.1
```

---

最终：

Tship′=∑iαitiT'_{ship} = \sum_i \alpha_i t_iTship′​=i∑​αi​ti​

---

即：

动态组合prompt。

---

效果：

对于港口图：

```
Ship Prompt≈ cargo ship
```

---

对于军港图：

```
Ship Prompt≈ warship
```

---

这是：

Dynamic Prompt

Multi-Prototype Prompt

同时具备。

---

我觉得这是最符合你前面提出的：

> 一个类别多个prompt

的升级版。