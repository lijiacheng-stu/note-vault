对gate fusion module（GFM）：
- $X^i_R$ 和$X^{i-1}_R$ 的空间分辨率是否相同？ 或 $X^i_{RDcom}$ 和$X^{i-1}_{RD}$ 的空间分辨率是否相同？
- gate 不是 一个通道一个数字那种了吗？

对global context module （GCM）：



对multilayer aggregation decoder (MAD):


step1：每个位置相对于其他所有位置的注意力。
step2: 得到每个位置的值。现在需要提供伟哥位置的值，就能得到最终的值。
step3: 每个位置的值 = 注意力 x 值



## 对编码器解码器结构的一个总结

### 为什么要采用编码器解码器架构
- 

### 对编码器解码器的结构的归纳
#### 理论归纳
在语义分割任务中，常用编码器解码器架构。编码器负责提取多尺度特征；解码器通过逐级上采样，并与编码器不同阶段提取的特征进行融合，获得兼具空间细节与语义信息的多尺度融合特征。多尺度融合特征经过预测头处理，得到最终的像素级分类结果。
以下是经典编码器解码器的结构：
![[Pasted image 20260725155508.png]]
- 编码器：
	- 自下而上的路径(Bottom-up pathway)： 经过骨干网络的前馈计算，得到多尺度特征，其中浅层特征保留丰富的空间细节，深层特征包含较强的语义信息；
- 解码器
	- 自上而下的路径(Top-down pathway)：通过上采样，让空间上更粗糙，语义上更强的自上而下的特征得到更高的分辨率。使其能够与空间信息更丰富语义信息更弱的自下而上的特征融合。
	- 横向连接(lateral connection)：融合空间分辨率相同的自下而上特征与上采样后的自上而下特征，得到空间信息丰富，语义信息强的融合特征。
	- 预测头：利用兼顾目标边界细节和语义表达能力的融合特征，生成与输入图像尺寸一致的像素级预测结果。
这张图看似非常简单，但是内涵确非常丰富。从设计模型的角度，无论是自下而上的特征提取，自上而下的上采样，横向连接的融合特征，预测头的生成预测结果，都可以采用不同的不同的模块，这样就海量的组合变化进行调试。此外，根据不同应用场景的需求，可以对这一结构进行剪裁，用以权衡准确率与计算效率。
从模型分析的角度，可以把任意采用这一架构的模型，都解构成这四个部分，实现模型结构的梳理。基于此，便于模型优化、各模型间进行横向对比。这就形成了一个分析框架。
接下来，用这个简单的图，用这四种结构解构，对我当前看过的所有的采用编码器解码器架构的模型进行模型分析。

#### 插曲

问题：UNet、FPN、encoder-decoder achitecture都采用“多尺度特征融合”的思想， 三者架构几乎完全相同。FPN为什么能在Unet后发表文章？（UNet、FPN、encoder-decoder achitecture之间的关系）
- FPN，设计的初衷是用于目标检测，目的是一套具有不同尺度、各个尺度兼顾空间细节和语义信息的特征金字塔，这套特征为下游的目标检测奠定基础。
- encoder-decoder achitecture， 是一个抽象范式，在语义分割任务中，其目的是得到一个兼顾空间细节和语义信息的多尺度融合特征，这一特征为下游得到像素级的预测结果奠定基础。
- Unet，采用的是encoder-decoder achitecture，它们之间是实例和类的关系。

#### 模型分析
我看过的采用这种结构的网络有五个, 单模态有UNet, UperNet, DeepLabv3+，多模态有MGFNet, CEGFNet（这是这周新看的）
- U-Net：2015![[Pasted image 20260725140453.png]]
	- 自下而上：对应特征提取，结果是得到多尺度特征
	- 。普通卷积 + 最大池化
	- 自上而下：对应上采样。 转置卷积（上采样卷积）
	- 横向连接：对应特征融合。 拼接 + 普通卷积 
	- 预测头：对应最终多尺度融合特征处理成最终预测结果。 `1 * 1` 卷积
	- 评价：从现在的眼光看好像没什么特别的，但是当时确非常特别。Unet第一次将横向连接实现多尺度特征融合引入编码器解码器架构，并在医学图像语义分割任务中取得突破性效果。做了开创性的工作。
- UperNet: 2018 (特征金字塔是2016)![[Pasted image 20260725141118.png]]
	- 对FPN的理解，以及区分Unet。FPN设计的初衷是通过得到得到一套具有不同尺度、各个尺度兼顾空间细节和语义信息的特征金字塔，这套特征为下游的目标检测奠定基础。所以，FPN的作用是特征提取，所以它是编码器。
	- 自下而上：FPN
	- 自上而下和横向连接：上采用，统一多尺度特征分辨率，而后拼接。
	- 预测头：`3 * 3`卷积 + `1 * 1` 卷积
	- 评价：对原编码器解码器架构在自上而下和横向连接上进行了裁剪。因为经过在编码器阶段后，采用FPN使得各尺度特征均兼顾了空间细节和语义信息，因此，这种裁剪具有可行性。
- DeepLabv3+ : 2018
![[Pasted image 20260725141703.png]]


- MGFNet：2024
![[Pasted image 20260725140111.png]]

- CEGFNet: 2021
![[Pasted image 20260725141921.png]]
#### 初步发现与结论
目前所有的采用编码器解码器结构的网络，都可以用经典编码器解码器网络中的四部分进行解构。这个解构方法，为比较不同的网络之间差异提供框架。




改进的方向点有很多：
- 对编码器进行修改
- 对解码器进行修改
- 对横向连接进行修改

对解码器进行的修改：
- ResNet， Xception，HRNet
- Atrous Convolution

对解码器进行的修改：
- 仅包含预测头
- 跨层级

文章采用了

- 多尺度特征：这里的尺度是空间尺度



框架：
- 对遥感图像进行语义分割，和传统图像相比，困难在哪儿里？解决问题的方向在哪儿？
- 有哪儿些网络使用了编码器解码器架构？ 为什么编码器解码器架构非常适合遥感图像语义分割？
- 这些网络编码器是如何设计的？解码器是如何设计的？


## 对问题2




## GFM：
1. 他的名字叫什么？
门融合模块


2. 它的作用是什么？
与双流骨干网络结合，用于提取双模态之间的互补特征。即， $X^i_R$ 


3. 他的输入是什么？


4. 他的输出是什么？
5. 起作用的原理是什么？



## GCM：
1. 他的名字叫什么？
全局上下文模型


2. 它的作用是什么？
与双流骨干网络结合，用于提取双模态之间的互补特征。即， $X^i_R$ 


3. 他的输入是什么？


4. 他的输出是什么？
5. 起作用的原理是什么？





## MAD：
1. 他的名字叫什么？
多层聚合解码器


2. 它的作用是什么？
与双流骨干网络结合，用于提取双模态之间的互补特征。即， $X^i_R$ 


3. 他的输入是什么？


4. 他的输出是什么？
5. 起作用的原理是什么？




## PTM：
1. 他的名字叫什么？
预测终端模块


2. 它的作用是什么？
与双流骨干网络结合，用于提取双模态之间的互补特征。即， $X^i_R$ 


3. 他的输入是什么？


4. 他的输出是什么？
5. 起作用的原理是什么？