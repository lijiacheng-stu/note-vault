- 英语中，一个句子中不同单词之间存在空格，空格和词元之间是什么关系？
- 现在给decoder-only model输入一个context，最终生成一个（context length，vocabulary size）的一个矩阵用于表示每个token预测的下一个token是什么，即第i个token，预测i+1个。如“我爱中国”中，我应该预测到爱，可是爱真的是最大概率吗？不一定吧。
- 残差连接对梯度的影响？反向传播和链式法则？
- 训练和推理序列最大序列长度不一致会怎样？在训练的时候，会设置max length这种超参数，然后输入不定长的需要后，可以通过padding token实现长度对齐，使用padding mask避免token 关注padding token的key。推理的时候，如果说输入的序列比max length长或短都是没有问题的。因为无论是MLP，还是MHA都对长度没有要求，而且没有padding了呀。可能唯一的限制就是绝对位置编码只训练了max length那么多个。