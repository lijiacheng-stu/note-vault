索引 (n,m,k), 表达的是3个索引，n表示第二个和第三个索引值确定时，第一个索引可以有的索引数量。他们之间可以有任何顺序的变化，且可保持语义不变。
可以把其中的k，横过来。
可以把n，m，弄成二维度
可以把n，m，k，弄成三维度

计算某个维度的均值：固定某个维度的值，其他任意变化，求均值。
可以求两个维度的均值吗？固定两个维度的值，再看变化。


值层面


存储的不是数字，而是这个维度的数量。
