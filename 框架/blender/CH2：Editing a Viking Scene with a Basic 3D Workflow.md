1. 规范：
	- 纹理文件应该单独放一个文件夹 .blend files can also include image textures, though most 3D artists choose to keep the texture files separate so that the .blend file will take up less room on the computer.
2. 碎碎念：
	- 怎样打开一个源文件？通用的做法是打开想打开这个源文件的软件，然后再`file>open` 的类似物中打开对应的文件。


