- 这里用final好不好呢？
![[Pasted image 20250503164220.png]]
MainController是Singleton的，构造器只会使用一次，因此，不同的用户同时访问时，会访问相同的MainController，且是相同的LoggedUserMannagementService 。可是不同的用户我们需要不同的LoggedUserMannagementService。
而且LoggedUserMannagementService应该依赖于request的存在，而Controller在context定义的时候就生成了，这会不会导致太早呢？

也就是说，在singleton中如何获得request scop bean的instance