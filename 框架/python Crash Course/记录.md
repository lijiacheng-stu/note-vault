一、一些自带的方法
- sorted()
- len()
- list()
- range()
- print()
- min()
- max()
- sum()
- set()
- int()
	```
	The int() function converts a string representation of a number to a numerical representation
	int数字的字符串表示在输出的时候与数字表示可以等同，但是不能进行算数和比较运算。
	```
- type()
- isinstance(x, int)
	
 二、关键字
 条件判断：
 - and
 - or

- in
- not in


三、数据类型
内置:
- string
- number
- list
- tuple
- boolean
- dictionary
- set
注意：
- list和dictionary是有序的，set是无序的

四、输出不是true和false，但是条件判断中是true和false的情况？
- `[]` 为false， `[message]` 为true
- `''或""` 为false， `'message'或"message"`为true
- 

五、一些模块
- random
- json

输入、输出。
变量。
字符串、数字、布尔类型数据。
数组
if statement，loop statement。
如何表示一个对象。


