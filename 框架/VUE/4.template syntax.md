`v-html`"keep this element's inner HTML up-to-date with the `rawHtml` property on the current active instance.
`v-bind` directive is used to reactively update an HTML attribute
`v-on` directive, which listens to DOM events
`v-if` directive would remove or insert the `<p>` element based on the truthiness of the value of the expression `seen`.


Foxit Software\Foxit PDF Reade