#### 一、创建一个vue项目
- 在需要创建文件的地方，打开终端，运行此命令，生成项目
`npm create vue@latest` 
- 下载相关依赖,在vscode中打开项目后，执行以下命令
`npm i`
- 运行项目
`npm run dev`
- 使用setup语法糖，安装插件
`npm i vite-plugin-vue-setup-extend`
在`vite.config.ts`中配置该插件：
```
import vueSetupExtend from 'vite-plugin-vue-setup-extend'
vueSetupExtend()
```
