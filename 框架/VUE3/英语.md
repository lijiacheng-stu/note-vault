普通：
- micro 微观
- macro 宏观
- arbitrary 任意的
- replicate 复现；复制
- fallback 备用方案
- ascend上升 -> descend下降 -> descendant后代
专业：
- entry file 入口文件
- side effects:
 API request or DOM modification
-  An async component: 
 关键词：“async operation” and the “need for it to be completed,”
 具体定义：is “_a component that requires an asynchronous function to be performed and completed before the rendering can_ _be initialized_.”
- overhead：
 a cost that does not directly produce revenue
 - nested structure 嵌套结构
 - hot reload
 This will allow us to make modifications to our application while the E2E test runner is ready to run.
 - anthor 锚点 `<a></a>`
 - 